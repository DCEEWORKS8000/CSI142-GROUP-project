public class Members extends Person {


        private int memberId
        private double totalContribution;
    
        public Member(int memberId,String name,String phoneNumber){
                     super(name,phoneNumber);
                     this.memberId=memberId;
                     this.totalContributions=0;
    
       public int getMemberId(){
          return memberId;

        public double getTotalContribution() {
            return totalContribution;
        }
    
    
        public void contribute(double amount) {
            if (amount > 0) {
                totalContribution += amount;
            } else {
                System.out.println("Contribution must be positive.");
            }
        }
           
        @Override
        public String toString() {
            return "Member: " + name + ,"ID:"+ memberId + ,"Phone number" +phoneNumber+ "Total Contribution: " + totalContribution;
        }
    }
    
}
